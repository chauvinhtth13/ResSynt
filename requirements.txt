# Core Django
Django==5.0.1  # Or Django>=4.2 for native psycopg3 support
django-environ==0.11.2

# Database - psycopg3
psycopg[binary]==3.2.3  # psycopg3 with binary
psycopg[pool]==3.2.3    # Optional: connection pooling support

# If using older Django (<4.2), you might need:
# django-psycopg3-adapter==1.0.0  # Adapter for Django <4.2

# Security
django-axes==6.1.1


# Caching & Task Queue
django-redis==5.4.0
redis==5.0.1
celery[redis]==5.4.0  # Task queue with Redis support
vine==5.1.0  # Required by celery
kombu==5.3.5  # Messaging library
amqp==5.2.0  # AMQP protocol
billiard==4.2.0  # Process pool

# Utilities
python-dateutil==2.8.2
pytz==2023.3

# Production
gunicorn==21.2.0
whitenoise==6.6.0

# Optional
sentry-sdk==1.39.1
tabulate==0.9.0  # For pretty command output