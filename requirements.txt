# =============================================================================
# ResSynt - Research Data Management Platform
# Requirements file
# =============================================================================

# =============================================================================
# Django Core
# =============================================================================
Django>=5.0,<6.0

# =============================================================================
# Django Authentication & Security
# =============================================================================
django-allauth>=65.0.0          # Authentication (allauth, allauth.account, allauth.usersessions)
django-axes>=7.0.0              # Brute-force protection
django-csp>=4.0                 # Content Security Policy

# =============================================================================
# Django Extensions & Utilities
# =============================================================================
django-environ>=0.11.0          # Environment variable configuration
django-extensions>=3.2.0        # Shell plus, management commands
django-health-check>=3.18.0     # Health check endpoints
whitenoise>=6.7.0               # Static file serving

# =============================================================================
# Database
# =============================================================================
psycopg[binary]>=3.2.0          # PostgreSQL adapter (psycopg3)

# =============================================================================
# Password Hashing (required for PASSWORD_HASHERS)
# =============================================================================
argon2-cffi>=23.1.0             # Argon2 password hasher

# =============================================================================
# Task Queue
# =============================================================================
celery>=5.4.0                   # Async task queue
kombu>=5.4.0                    # Messaging library for Celery

# =============================================================================
# Redis (Production cache & Celery broker)
# =============================================================================
redis>=5.0.0                    # Redis client
django-redis>=5.4.0             # Django Redis cache backend

# =============================================================================
# Logging
# =============================================================================
concurrent-log-handler>=0.9.25  # Thread-safe rotating file handler

# =============================================================================
# Data Processing
# =============================================================================
pandas>=2.2.0                   # Data analysis
openpyxl>=3.1.0                 # Excel file handling
python-docx>=1.1.0              # Word document generation (TMG Report)
reportlab>=4.0.0                # PDF document generation
tabulate>=0.9.0                 # Table formatting
python-dateutil>=2.9.0          # Date utilities (dateutil.relativedelta)

# =============================================================================
# Development Tools (optional)
# =============================================================================
django-debug-toolbar>=4.4.0     # Debug toolbar (dev only)

# =============================================================================
# Production Server
# =============================================================================
gunicorn>=23.0.0                # WSGI HTTP Server

# =============================================================================
# Encrypted Fields - FOR PII DATA
# =============================================================================
# Using django-fernet-encrypted-fields for field-level encryption
# Requires: FERNET_KEYS and SALT_KEY in settings
django-fernet-encrypted-fields>=0.3.1
